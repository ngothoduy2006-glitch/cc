"use strict";
import { jsx, jsxs } from "react/jsx-runtime";
import { history } from "@umijs/max";
import { motion } from "framer-motion";
import {
  ArrowUpOutlined,
  MessageOutlined,
  EyeOutlined,
  ClockCircleOutlined,
  UserOutlined
} from "@ant-design/icons";
import TagBadge from "./TagBadge";
import dayjs from "dayjs";
import relativeTime from "dayjs/plugin/relativeTime";
dayjs.extend(relativeTime);
const roleMap = {
  student: { label: "Sinh vi\xEAn", color: "#4F8CFF" },
  lecturer: { label: "Gi\u1EA3ng vi\xEAn", color: "#7B61FF" },
  admin: { label: "Admin", color: "#FF6B6B" }
};
export default function PostCard({ post, index = 0 }) {
  const role = post.author?.role ? roleMap[post.author.role] : roleMap.student;
  return /* @__PURE__ */ jsxs(
    motion.div,
    {
      className: "glass-card p-5 cursor-pointer group",
      style: {
        background: "var(--glass-bg)",
        border: "1px solid var(--glass-border)"
      },
      onClick: () => history.push(`/post/${post.id}`),
      initial: { opacity: 0, y: 20 },
      animate: { opacity: 1, y: 0 },
      transition: { duration: 0.35, delay: index * 0.05, ease: "easeOut" },
      whileHover: { y: -3, boxShadow: "0 16px 48px var(--shadow)" },
      children: [
        /* @__PURE__ */ jsx("div", { className: "flex flex-wrap gap-2 mb-3", children: post.tags?.map((tag) => /* @__PURE__ */ jsx(TagBadge, { tag, onClick: (e) => {
          e.stopPropagation();
          history.push(`/forum?tag=${tag.id}`);
        } }, tag.id)) }),
        /* @__PURE__ */ jsx(
          "h3",
          {
            className: "font-bold text-base leading-snug mb-2 line-clamp-2 transition-colors duration-200 group-hover:text-primary-500",
            style: { color: "var(--text)" },
            children: post.title
          }
        ),
        /* @__PURE__ */ jsx(
          "p",
          {
            className: "text-sm leading-relaxed mb-4 line-clamp-2",
            style: { color: "var(--text-muted)" },
            dangerouslySetInnerHTML: {
              __html: post.content.replace(/<[^>]+>/g, "").slice(0, 160)
            }
          }
        ),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between flex-wrap gap-2", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsx(
              "div",
              {
                className: "w-7 h-7 rounded-lg flex items-center justify-center text-white text-xs font-bold flex-shrink-0",
                style: { background: `linear-gradient(135deg, ${role.color}, ${role.color}99)` },
                children: post.author?.name?.charAt(0) || /* @__PURE__ */ jsx(UserOutlined, {})
              }
            ),
            /* @__PURE__ */ jsxs("div", { children: [
              /* @__PURE__ */ jsx("span", { className: "text-xs font-medium", style: { color: "var(--text)" }, children: post.author?.name || "\u1EA8n danh" }),
              /* @__PURE__ */ jsx(
                "span",
                {
                  className: "ml-1.5 text-xs px-1.5 py-0.5 rounded font-semibold text-white",
                  style: { background: role.color, fontSize: "0.65rem" },
                  children: role.label
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 text-xs", style: { color: "var(--text-muted)" }, children: [
            /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx(
                ArrowUpOutlined,
                {
                  style: {
                    color: post.votes > 0 ? "#4F8CFF" : post.votes < 0 ? "#ef4444" : "var(--text-light)"
                  }
                }
              ),
              /* @__PURE__ */ jsx(
                "span",
                {
                  style: { color: post.votes > 0 ? "#4F8CFF" : post.votes < 0 ? "#ef4444" : "var(--text-muted)" },
                  className: "font-semibold",
                  children: post.votes
                }
              )
            ] }),
            /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx(MessageOutlined, {}),
              /* @__PURE__ */ jsx("span", { className: "font-medium", children: post.answersCount })
            ] }),
            /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx(EyeOutlined, {}),
              /* @__PURE__ */ jsx("span", { children: post.views })
            ] }),
            /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsx(ClockCircleOutlined, {}),
              /* @__PURE__ */ jsx("span", { children: dayjs(post.createdAt).fromNow() })
            ] })
          ] })
        ] })
      ]
    }
  );
}
