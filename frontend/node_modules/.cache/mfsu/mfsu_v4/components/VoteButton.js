"use strict";
import { jsx, jsxs } from "react/jsx-runtime";
import { motion, AnimatePresence } from "framer-motion";
import { ArrowUpOutlined, ArrowDownOutlined } from "@ant-design/icons";
export default function VoteButton({ count, userVote, onVote, disabled, vertical = true }) {
  return /* @__PURE__ */ jsxs("div", { className: `flex ${vertical ? "flex-col" : "flex-row"} items-center gap-1`, children: [
    /* @__PURE__ */ jsx(
      motion.button,
      {
        onClick: () => !disabled && onVote("up"),
        className: `vote-btn ${userVote === "up" ? "active-up" : ""}`,
        disabled,
        whileTap: { scale: 0.85 },
        whileHover: { scale: 1.15 },
        style: { cursor: disabled ? "not-allowed" : "pointer" },
        title: "H\u1EEFu \xEDch",
        children: /* @__PURE__ */ jsx(ArrowUpOutlined, { style: { fontSize: 18 } })
      }
    ),
    /* @__PURE__ */ jsx(AnimatePresence, { mode: "wait", children: /* @__PURE__ */ jsx(
      motion.span,
      {
        className: "font-bold text-base min-w-[2rem] text-center",
        style: {
          color: userVote === "up" ? "#4F8CFF" : userVote === "down" ? "#ef4444" : "var(--text-muted)"
        },
        initial: { opacity: 0, y: userVote === "up" ? -6 : 6 },
        animate: { opacity: 1, y: 0 },
        exit: { opacity: 0 },
        transition: { duration: 0.2 },
        children: count
      },
      count
    ) }),
    /* @__PURE__ */ jsx(
      motion.button,
      {
        onClick: () => !disabled && onVote("down"),
        className: `vote-btn ${userVote === "down" ? "active-down" : ""}`,
        disabled,
        whileTap: { scale: 0.85 },
        whileHover: { scale: 1.15 },
        style: { cursor: disabled ? "not-allowed" : "pointer" },
        title: "Kh\xF4ng h\u1EEFu \xEDch",
        children: /* @__PURE__ */ jsx(ArrowDownOutlined, { style: { fontSize: 18 } })
      }
    )
  ] });
}
