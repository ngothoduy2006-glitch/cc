"use strict";
import { jsx, jsxs } from "react/jsx-runtime";
import { motion, AnimatePresence } from "framer-motion";
import {
  CheckCircleFilled,
  CloseCircleFilled,
  InfoCircleFilled,
  WarningFilled,
  CloseOutlined
} from "@ant-design/icons";
import { useUIStore } from "@/stores/ui";
const icons = {
  success: /* @__PURE__ */ jsx(CheckCircleFilled, { style: { fontSize: 18 } }),
  error: /* @__PURE__ */ jsx(CloseCircleFilled, { style: { fontSize: 18 } }),
  info: /* @__PURE__ */ jsx(InfoCircleFilled, { style: { fontSize: 18 } }),
  warning: /* @__PURE__ */ jsx(WarningFilled, { style: { fontSize: 18 } })
};
const colors = {
  success: "rgba(34,197,94,0.92)",
  error: "rgba(239,68,68,0.92)",
  info: "rgba(79,140,255,0.92)",
  warning: "rgba(245,158,11,0.92)"
};
export default function Toast() {
  const { toasts, removeToast } = useUIStore();
  return /* @__PURE__ */ jsx("div", { className: "toast-container", children: /* @__PURE__ */ jsx(AnimatePresence, { children: toasts.map((toast) => /* @__PURE__ */ jsxs(
    motion.div,
    {
      className: "toast-item",
      style: { background: colors[toast.type] },
      initial: { opacity: 0, x: 80, scale: 0.95 },
      animate: { opacity: 1, x: 0, scale: 1 },
      exit: { opacity: 0, x: 80, scale: 0.95 },
      transition: { type: "spring", stiffness: 300, damping: 25 },
      children: [
        /* @__PURE__ */ jsx("span", { className: "flex-shrink-0", children: icons[toast.type] }),
        /* @__PURE__ */ jsx("span", { className: "flex-1 text-sm", children: toast.message }),
        /* @__PURE__ */ jsx(
          "button",
          {
            onClick: () => removeToast(toast.id),
            className: "flex-shrink-0 opacity-70 hover:opacity-100 transition-opacity",
            children: /* @__PURE__ */ jsx(CloseOutlined, { style: { fontSize: 12 } })
          }
        )
      ]
    },
    toast.id
  )) }) });
}
