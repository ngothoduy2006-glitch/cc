"use strict";
import { jsx, jsxs } from "react/jsx-runtime";
import { useEffect, useState } from "react";
import { motion } from "framer-motion";
import { BookmarkFilled } from "@ant-design/icons";
import { postsAPI } from "@/services/api";
import PostCard from "@/components/PostCard";
import { PostCardSkeleton } from "@/components/LoadingSkeleton";
import { useAuthStore } from "@/stores/auth";
import { history } from "@umijs/max";
export default function SavedPostsPage() {
  const { isAuthenticated } = useAuthStore();
  const [posts, setPosts] = useState([]);
  const [loading, setLoading] = useState(true);
  useEffect(() => {
    if (!isAuthenticated) {
      history.push("/auth/login");
      return;
    }
    postsAPI.getSavedPosts().then((r) => setPosts(r.data || [])).catch(() => {
    }).finally(() => setLoading(false));
  }, [isAuthenticated]);
  return /* @__PURE__ */ jsx("div", { className: "max-w-4xl mx-auto px-4 sm:px-6 py-8", children: /* @__PURE__ */ jsxs(motion.div, { initial: { opacity: 0, y: 20 }, animate: { opacity: 1, y: 0 }, children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3 mb-6", children: [
      /* @__PURE__ */ jsx(
        "div",
        {
          className: "w-10 h-10 rounded-xl flex items-center justify-center",
          style: { background: "linear-gradient(135deg, #4F8CFF, #7B61FF)" },
          children: /* @__PURE__ */ jsx(BookmarkFilled, { style: { color: "#fff", fontSize: 18 } })
        }
      ),
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h1", { className: "text-2xl font-black", style: { color: "var(--text)" }, children: "B\xE0i \u0111\xE3 l\u01B0u" }),
        /* @__PURE__ */ jsxs("p", { className: "text-sm", style: { color: "var(--text-muted)" }, children: [
          posts.length,
          " c\xE2u h\u1ECFi"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "space-y-4", children: loading ? Array.from({ length: 3 }).map((_, i) => /* @__PURE__ */ jsx(PostCardSkeleton, {}, i)) : posts.length === 0 ? /* @__PURE__ */ jsxs("div", { className: "text-center py-16 rounded-2xl border", style: { background: "var(--surface)", borderColor: "var(--border)" }, children: [
      /* @__PURE__ */ jsx("div", { className: "text-5xl mb-4", children: "\u{1F516}" }),
      /* @__PURE__ */ jsx("h3", { className: "font-bold text-lg mb-2", style: { color: "var(--text)" }, children: "Ch\u01B0a l\u01B0u b\xE0i n\xE0o" }),
      /* @__PURE__ */ jsx("p", { className: "text-sm mb-4", style: { color: "var(--text-muted)" }, children: "L\u01B0u c\xE1c c\xE2u h\u1ECFi hay \u0111\u1EC3 xem l\u1EA1i sau" }),
      /* @__PURE__ */ jsx(
        "button",
        {
          onClick: () => history.push("/forum"),
          className: "px-6 py-2.5 rounded-xl text-white font-semibold text-sm",
          style: { background: "linear-gradient(135deg, #4F8CFF, #7B61FF)" },
          children: "Kh\xE1m ph\xE1 di\u1EC5n \u0111\xE0n"
        }
      )
    ] }) : posts.map((p, i) => /* @__PURE__ */ jsx(PostCard, { post: p, index: i }, p.id)) })
  ] }) });
}
